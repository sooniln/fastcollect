/**
 * Methods for dealing with ${Type}Traversables.
 */
@file:JvmName("${Type}Traversables")
@file:JvmMultifileClass

package io.github.sooniln.fastcollect

import kotlin.jvm.JvmMultifileClass

public fun ${Type}Traversable.foreach(action: ${Type}Consumer): Unit = foreach { action.accept(it) }
